{
  "name": "stats",
  "version": "1.0.0",
  "description": "cvs x tsc",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "//comment1": "below script will execute concurrently on all npm scripts that start with 'start'",
    "start": "concurrently npm:start:*"
  },
  "author": "darkwingduckcurry",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.6.2",
    "concurrently": "^7.3.0",
    "nodemon": "^2.0.19"
  }
}
